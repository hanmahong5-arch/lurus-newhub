# Cycle 15 — 控制台对标 OpenRouter 的全面提升 (2026-09-22)

## 对标对象与理由

**主对标 = OpenRouter**(openrouter.ai)。理由:它是「一个 API 接全部模型」这一品类的头部产品,
我们的控制台核心页面(模型、排行、用量、令牌、调试台)与它一一对应;它的模型广场/排行榜/
Activity 是该品类公认的标杆。**企业治理面**(日志、审计、网关健康)参考 Helicone / Portkey 的
仪表盘形态(时间范围切换、错误分布、Top 模型)。

约束:品牌色/字体来自平台同步的 `web/src/styles/lurus-design/tokens.css`(`--lt-*`),本轮**不改品牌
token**,只在 `--hf-*` 产品层和组件层提升;前端一律 Bun;每个阶段单独 PR、先本地测透再上 UAT。

## 基线(2026-09-22 UAT 实拍,26 页)

截图:`C:/Users/Anita/newhub-design/before/*.png`(不入库)。实拍发现的具体缺陷:

| # | 页面 | 缺陷 | 性质 |
|---|---|---|---|
| D1 | 全局 | `.lbl` / `.tag` / `th` / `.nav-h` 的大写字距 0.08–0.16em 作用到中文上,「近 5 分钟」「我 的 账 户」被拉散 | 视觉 |
| D2 | 用量与日志 | 表格窄列按字符竖排(表头「上游」、状态「错误」、「暂无」各占两行) | 视觉/可读性 |
| D3 | 模型 | UAT 有 2 个可路由模型,模型页显示「0 个模型」——目录只读 `models` 元数据表,不读实际可路由集合 | 充实度 |
| D4 | 渠道 | 「类型」列显示原始数字 `1`、`36`,没有供应商名称和图标 | 充实度 |
| D5 | 排行榜 | 空 `model_name`(被拒请求)显示为空白名字行;只有表格,没有趋势图 | 充实度 |
| D6 | 定价 | v2 定价接口丢掉了 `completion_ratio`/`description`/`icon`/`tags`,「完成倍率」列恒空 | 功能 |
| D7 | 概览 | 大面积「—」空态;用量趋势只有单色柱,无按模型拆分、无时间范围切换 | 充实度 |

## 阶段(每阶段 = 1 个 PR,验证方法写在每条后面)

### P1 视觉地基 + 数据补全(本 PR)
1. D1:中文环境下取消标签类字距(`html[lang^='zh']` 作用域)。验证:截图对比。
2. D2:表头/标签/状态不折行;中文 `word-break: keep-all`。验证:截图对比 Log 页。
3. 供应商图标组件 `HfVendorIcon`(复用 `@lobehub/icons`)。验证:单测渲染 + 截图。
4. D4:渠道类型显示名称 + 图标。验证:单测 + 截图。
5. D6:v2 定价接口补 `completion_ratio`/`description`/`icon`/`tags`。验证:Go 单测(变异:删字段即红)。
6. D5:排行榜空名显示「未识别模型」。验证:单测。

### P2 模型广场(OpenRouter /models)
- 数据 = 可路由模型 ∩ 租户定价 ∪ 元数据:卡片(图标、名称、供应商、描述、输入/输出 $/1M、缓存价、
  能力标签、近 7 天 tokens)、搜索、供应商/能力筛选、排序(名称/价格/热度)、列表/表格切换、
  详情抽屉(价格明细、接入代码、去调试台)。管理员的增删改保留。
- 验证:页面测试断言「可路由但无元数据的模型也出现」(D3 变异:只读元数据即红)+ UAT 截图。

### P3 活动与排行(OpenRouter /activity + /rankings)
- 概览:时间范围 24H/7D/30D;按模型堆叠的每日消费图;Top 模型;最近请求。
- 排行榜:按周堆叠趋势图 + 排行表(份额条、环比)。
- 验证:页面测试 + UAT 截图。

### P4 日志与账单打磨(Helicone 形态)
- 日志:状态分布条、错误码聚合、请求详情分区(请求/响应/计费/链路)。
- 账单:余额卡、消费构成、充值记录时间线。

## 完成判据
每阶段:vitest 全绿(已知负载超时除外并注明)、prettier/eslint、`bun run build`、bundle budget、Go 相关包
测试;上 UAT 后对同一组路由重拍截图,与基线逐页对比贴进 PR。

## 实际交付(2026-09-22,与上面的计划对照)

| 阶段 | PR | 与计划的偏差 |
|---|---|---|
| P1 | #196 | 按计划。另:日志「状态」列挪到时间旁(原先被详情栏挤出视野)、去掉恒为 .000 的毫秒;渠道表单「类型」由手填整数改为带名称下拉 |
| P2 | #197 | 按计划。首次推送时 CI 抓到 `hooks/common/self_tenant_alias_page.test.jsx` 渲染真 Models 页而 mock 缺 `getServerAddress`——本地只跑了 pages/v2,**之后每阶段推送前改跑全量** |
| P3 | #198 | 概览:按模型堆叠 + 指标切换(消费/token/请求)+ 7 天/窗口切换;**没做** 24H(数据源按小时聚合,但窗口常量是 29 天,24H 视图留给 P5)。排行榜只加了份额条,**没做**按周堆叠趋势——排行榜接口只有聚合没有时间序列 |
| P4 | 本 PR | 日志头补 token 输入→输出、缓存读取(**不算命中率**:各家对 prompt_tokens 是否含缓存口径不同);侧边栏两组管理区可折叠。**没做**:日志状态分布条(列表只有一页样本,做出来是误导)、错误码聚合(已有「错误聚类」页签)、账单页改版 |

## 功能规划:对标 OpenRouter 仍缺的部分(按价值排序,均未立项)

1. **模型详情独立页**(OpenRouter `/:vendor/:model`):上下文长度、发布时间、按渠道的延迟/吞吐/可用率。
   缺口在数据:模型元数据表没有上下文长度字段;按渠道的延迟/可用率只有管理员可见(model-performance)。
   需要:元数据补字段 + 一个租户可读的按模型性能接口。
2. **模型对比**(Compare):广场里多选 → 并排价格/能力表。纯前端,数据已齐。
3. **余额与充值流**(Credits):充值依赖平台计费(UAT 关闭),先做余额历史时间线。
4. **按小时的 24H 活动视图**:`/api/data/self/` 本身按小时,需把窗口从固定 29 天改为可选。
5. **应用/来源归因**(OpenRouter「Apps」):已有 `X-Lurus-Product` 归因(日志「按产品拆分」),可做成排行。
6. **排行榜时间序列**:后端 `GetRankings` 加按天分组,前端复用 HfActivityChart。
