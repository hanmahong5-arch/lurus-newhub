dist
node_modules

# Vendored brand design tokens — generated by the platform's design-sync
# script and verified byte-for-byte by its --check mode. Reformatting them
# here would make that drift check fail forever.
src/styles/lurus-design
