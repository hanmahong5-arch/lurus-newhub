// Package gates holds structural tests that span the whole repository
// rather than one package: today the Go source-size ratchet. There is no
// production code here; the package exists so the gates have a home that
// does not belong to any one lane.
package gates
